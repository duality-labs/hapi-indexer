{
  "name": "duality-hapi-indexer",
  "version": "0.1.0",
  "description": "A Node.js based indexer for the Duality Cosmos chain",
  "main": "index.js",
  "scripts": {
    "docker": "touch .env.local && docker-compose up --build --abort-on-container-exit || true && docker-compose down",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js"
  },
  "dependencies": {
    "@hapi/hapi": "^21.3.1",
    "sqlite3": "^5.1.6",
    "winston": "^3.8.2"
  }
}
